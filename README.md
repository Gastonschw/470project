# ResuRank AI

AI-powered resume-to-job ranking system for CSCE 470. Takes a resume and ranks ~30k job postings by relevance using three strategies: TF-IDF, Semantic (sentence-transformers), and a Hybrid blend of both.

## Setup

Requires Python 3.10+. Install dependencies:

```bash
cd resurank-ai
pip install -r requirements.txt
```

Place the CareerBuilder LDJSON dataset in the parent directory (`470proj/`).

## How to Run

### Rank jobs for a resume

```bash
python test_core.py --data ../marketing_sample_for_careerbuilder_usa-careerbuilder_job_listing__20200401_20200630__30k_data.ldjson --resume ../Malik_Rabb_Resume.docx
```

Use `--demo` instead of `--resume` to run with a built-in sample resume. Optional flags: `--sample N` (default 500), `--top-k N` (default 10), `--alpha F` (hybrid weight, default 0.5).

### Run evaluation

Computes P@k, R@k, and AP for all 3 rankers against 3 resumes using LLM-generated ground truth labels:

```bash
python run_evaluation.py
```

Output:

```
Resume       Ranker        P@5   P@10    R@5   R@10     AP
----------------------------------------------------------
cody         TF-IDF      0.200  0.400  0.056  0.222  0.374
cody         Semantic    0.400  0.300  0.111  0.167  0.262
cody         Hybrid      0.400  0.300  0.111  0.167  0.363
malik        TF-IDF      0.200  0.200  0.038  0.077  0.302
malik        Semantic    0.400  0.300  0.077  0.115  0.274
malik        Hybrid      0.200  0.200  0.038  0.077  0.303
sriharsha    TF-IDF      0.600  0.500  0.188  0.312  0.643
sriharsha    Semantic    0.600  0.500  0.188  0.312  0.554
sriharsha    Hybrid      1.000  0.600  0.312  0.375  0.725
```

Ground truth was generated by prompting Claude to label 50 job postings per resume as relevant/irrelevant (`eval_cody.json`, `eval_malik.json`, `eval_sriharsha.json`).

## Project Structure

| File | Description |
|------|-------------|
| `data_loader.py` | Loads and cleans the LDJSON job dataset |
| `resume_parser.py` | Extracts text from PDF/DOCX resumes |
| `ranker.py` | TFIDFRanker, SemanticRanker, HybridRanker |
| `skill_extractor.py` | Skill matching and gap analysis |
| `evaluation.py` | P@k, R@k, AP metrics and eval helpers |
| `build_eval.py` | Generates eval datasets via LLM |
| `run_evaluation.py` | Runs all rankers on all resumes and prints metrics |
| `test_core.py` | End-to-end demo: rank jobs for a single resume |
